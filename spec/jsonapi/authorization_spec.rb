require 'spec_helper'

describe Jsonapi::Authorization do
  it 'has a version number' do
    expect(Jsonapi::Authorization::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
